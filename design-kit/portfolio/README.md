# Portfolio

External submission and sales-platform output packages.

- `kmong/`: Kmong portfolio hub with separate `mobile/` and `web/` packages.

Keep portfolio composites separate from `pub/` raw app artboards.
