# Assets

Shared visual inputs for design-kit pages.

- `generated/`: AI-generated or script-generated images used by `pub/` or portfolio pages.
- `brand/`: logos, marks, app icons, and brand lockups when available.
- `mockups/`: reusable device frames, masks, or platform-specific visual shells.

Current brand assets are not committed yet. Add only public-safe PLOZEN Control Tower assets here.

Do not store secrets or credential-bearing files here.
